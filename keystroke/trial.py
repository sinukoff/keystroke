import pandas as pd


class Trial(Object):
    def __init__(self, name, datafile_raw)
        self.name = name
        self.datafile_raw = datafile_raw

    def read_raw(self):
        pd.read_csv(self.datafile_raw)






